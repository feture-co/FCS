CREATE DATABASE IF NOT EXISTS bondhu_fund CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE bondhu_fund;

-- Sequelize creates and updates these tables with npm start or npm run seed.
-- Required tables:
-- users, members, deposits, dues, investments, profits,
-- deposit_requests, profit_distributions, transactions, notices, notifications,
-- audit_logs, push_subscriptions, Sessions.
